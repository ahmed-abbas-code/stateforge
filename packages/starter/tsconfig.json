{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "jsx": "preserve",
    "allowJs": true,
    "incremental": true,

    // Required for Node ESM + exports field
    "module": "Node16",
    "moduleResolution": "Node16",

    "types": ["node"]

    // DO NOT redefine baseUrl or paths here — inherited from tsconfig.base.json
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx"
  ],
  "exclude": ["node_modules"]
}
